// Student object
let student = {
    id: 101,
    name: "Pavana",
    age: 20,
    department: "AIML",
    marks: 90
};

// Print student details using template literals
console.log(`
Student Details
---------------
ID         : ${student.id}
Name       : ${student.name}
Age        : ${student.age}
Department : ${student.department}
Marks      : ${student.marks}
`);